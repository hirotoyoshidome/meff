const SETTING_FILEPATH*: string = "./settings.json"
type
  EXEC_TYPE* {.pure.} = enum
    HELP        = "help"
    CORRELATION = "correlation"
    GRAPH       = "graph"
const DATE_COL_NAME*: string = "date"

